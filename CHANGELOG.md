# Changelog

## 1.7.0

插件市场上架复审提出的问题修复，**默认安全边界收紧**（升级后行为有变化，见下）：

* **跨聊天命令默认仅管理员可用**：新增 `command.cross_chat_admin_only`（默认 `true`）。
  `/tw_all`（看所有聊天的订阅）、`/tw_del`（删所有聊天的订阅）、`/tw_reset`（重建基线）、
  `/tw_check`（触发全局轮询）、`/tw_interval`（改全局间隔）都会影响别的聊天，
  现在默认只有 `command.admins` 里的 QQ 与机器人的本地操作员（WebUI / 控制台）能用；
  被拒绝时会明确回复"这条命令会影响所有聊天的订阅或全局轮询状态…"。
  `/tw_sub`、`/tw_unsub`、`/tw_list`、`/tw_on|off`、`/tw_test` 只影响当前聊天，不受限制。
  > **升级提示**：如果你的部署原来靠"谁都能用"，升级后请把自己的 QQ 填进 `command.admins`，
  > 否则跨聊天命令会没人能用（本地操作员仍然可以）。

* **链接预览增加 SSRF 防护**：推文正文是外部内容，里面的链接可能指向
  `http://127.0.0.1:xxxx`、`192.168.x.x`、`169.254.169.254`（云 metadata）、
  `100.64.x.x`（CGNAT / Tailscale）等地址，抓回来的正文会被发进群。现在：
  * 只允许 `http(s)`；主机名不能是 `localhost` / `*.local` / `*.internal` 这类；
  * 域名**解析成 IP 逐个校验**，只要有地址不是公网（`not is_global`）就整条丢弃；
  * **手动跟随跳转并逐跳重新校验**（最多 5 跳），挡住"公网域名 302 到内网"；
  * 被拦下只写一条日志，推文照常推送；
  * 需要抓内网链接时显式打开 `link.allow_private_hosts = true`。
  > 顺带覆盖了 `http://2130706433/`（十进制 IP）这类绕过写法。

* **默认跳过敏感推文**：`push.skip_sensitive` 默认值由 `false` 改为 **`true`**，
  避免群里自动出现被接口标记为 `possibly_sensitive` 的内容。

* **可选依赖说明**：`trafilatura` / `bs4` / `readability` 一直是以"缺了也能跑"的方式导入的，
  现在补上启动自检日志（`链接正文提取器：…`，缺失时给出退化说明），并在 README 里
  明确标注为**可选增强、不写进 `manifest.dependencies`**——避免在别人机器上因为装不上
  重依赖而把插件整个卡住。三者都缺时退化为正则清洗 + `og:description`，Steam 公告走 RSS 不受影响。

* **仓库不再提交 `config.toml`**：该文件由 Runner 按插件的 `config_model` 在首次加载时生成，
  属于实例配置；仓库里只保留 README 里的配置说明，避免 `git pull` 冲突
  （`.gitignore` 已加入 `config.toml`）。

* 自测从 331 项扩到 **389 项**，新增 SSRF 用例（字面 IP / 内网域名 / 十进制 IP /
  IPv6 回环 / 非 http 协议 / 解析结果校验）、跨聊天权限用例（普通成员被拒、
  本地操作员与管理员放行、开关可关闭）以及默认安全值用例。

## 1.6.2

**大视频真的能发出去了（两条真正的拦路虎）**：

* **适配器 action 超时 10 秒太短**：`snowluma-adapter` 的 `action_timeout_sec` 默认 10.0，
  而 QQ 上传 298MB 视频要 ~39 秒，于是 `send_private_forward_msg` 必然超时、
  适配器断开重连、插件判失败并整条丢弃。现在部署侧把它调到 `180.0`
  （README「关键：把适配器的 action 超时调大」）。
* 新增 `media.video_keep_seconds`（默认 180）：容器里的视频文件**延迟 3 分钟**再删。
  适配器拿到的是容器内路径，NapCat 传大视频可能在 `send` 返回之后还在读这个文件，
  立刻删掉会让 QQ 那边收到失败或损坏的视频。本地临时文件依旧发完就删（已经拷进容器了）。
* 插件启动时会扫掉容器里超过「保留时长 ×2（至少 10 分钟）」的遗留视频
  （插件重载会取消挂着的延迟删除任务），卸载时也会取消这些任务。
* 启动日志新增 `视频大文件通道就绪：docker=… 容器=… 内联上限=… 外链上限=…`，
  每次视频节点发出后也会写一行 `聊天记录已发出，其中 N 个视频节点：<ID>=容器路径/内联`。

## 1.6.1

**修复「带视频的推文只发了封面」**：

* 根因是**插件在 systemd 服务里找不到 docker 命令**。Ubuntu 上 docker 由 snap 安装，
  命令只有 `/snap/bin/docker`，而服务的 `PATH` 里没有 `/snap/bin`，
  于是 `shutil.which("docker")` 返回 `None`，超过内联上限的视频借道容器的分支
  **被静默跳过**，最后只剩封面。
* 新增 `find_docker()`：先查 `PATH`，再依次兜底 `/snap/bin/docker`、`/usr/bin/docker`、
  `/usr/local/bin/docker`，结果带缓存；`docker exec` / `docker cp` 都改用它。
* **失败不再静默**：视频没发出去的每一种原因都会写一条日志——
  内联没有合适变体、内联下载失败、容器路由被关、找不到 docker（附带当时 `PATH`）、
  超过 `max_video_mb`、下载失败、`docker cp` 失败。
* 启动时多打一行视频通道自检：`视频大文件通道就绪：docker=… 容器=… 内联上限=…`
  （找不到 docker 时降级为 warning，直接说明超限视频只能发封面）。
* 部署侧：systemd unit 的 `PATH` 补上 `/snap/bin`。

## 1.6.0

聊天记录的形态与顺序统一：

* **最新的推文排在最上面**：以前聊天记录里是旧推文在前，现在是新推文在前。
* **单条推文也统一走聊天记录**：去掉了「单条纯文字发普通消息」的特例，
  一条推文同样是一条聊天记录、里面一个节点；单条视频推文则是「正文节点 + 视频节点」。
* 关掉 `display.use_forward` 时，聊天记录与「视频进节点」一起停用，回到纯图文 / 独立视频。
* 链接内容块收紧：**只有标题、没有正文**（抓到菜单页之类）时不再输出这个块，
  免得在推文下面贴一屏「投资者／财务信息／活动」。
* 菜单页识别增强：整页都是短条目、且没有任何一句像正文的长句时，判定为导航垃圾丢掉。

## 1.5.0

排版整理，外加两个内容修复：

* **排版重新分段**：作者头 + 分隔线 → 正文（含引用、配图说明）→ 链接内容 → 尾部（原推链接、互动数据），
  链接内容和尾部各自前面空一行，不再和正文糊成一片。
* 新增 `display.header_divider`：作者行下面的分隔线，留空可关闭。
* 新增 `display.hide_expanded_url`（默认开）：已经抓出正文的链接，不再在推文正文里重复出现。
* 正文里的多余空行会被收敛，避免原文的空行把消息撑得很散。
* **修复尖括号内容被当成 HTML 标签吃掉的问题**：官方的
  `play up to <Part 1> of Canto 10` 之前会被解析器吞掉，译文因此断成
  「玩到 Canto 10 的 / 部分」；现在只放行已知 HTML 标签（以及 `my-widget` 这类带连字符的自定义元素），
  其余尖括号内容一律按文本保留。
* **链接正文不再被砍半截**：上限从 700 字提到 3000 字（0 = 不限），
  并且改成「先翻全文再截断」——长文按段落分段翻译（默认每段 1200 字），
  避免一次输出撞上模型 `max_tokens` 被截掉后半段。
* 截断时尽量切在段落处，并明确标注「内容较长，已截断」。

## 1.4.0

推文里链接的正文也搬出来，并一起翻译：

* 新增 `[link]` 配置段：开关、每条推文最多展开几个链接、正文长度上限、超时、代理、是否翻译等。
* **Steam 新闻链接**走官方 RSS（页面正文是 JS 渲染的，直接抓只能抓到导航栏），
  按 URL 里的公告 ID 精确匹配；实测能拿到补丁说明全文，
  只有视频嵌入的公告则退化成「标题 + 🎬 视频链接」。
* 其它链接按 `trafilatura → readability → og:description` 逐级抽正文，
  并过滤「登录/隐私政策/版权所有」这类导航页脚垃圾，宁可不给也不刷一屏菜单。
* 抓到的正文按 `[translation]` 的设置一起翻译；本来就是中文的会自动跳过。
* **直连抓不到自动走代理重试**（实测 Steam 直连会超时，走代理 0.68 秒就回来了），
  代理复用 `[twitter] proxy`，也可用 `[link] proxy` 单独指定。
* 链接内容按 URL 缓存，同一条链接不会重复抓取与翻译。
* 抓取失败只是不加链接内容块，推文照常推送。

## 1.3.0

新增自动翻译：推文正文自动翻成中文，**用译文替换原文（不保留原语言正文）**。

* 新增 `[translation]` 配置段：开关、目标语言、模型任务、跳过规则、提示词、超时等。
* 默认用 `replyer`（回复模型）任务翻译，可换成 `utils` 等 `model_config.toml` 里的任意任务名。
  实测走的是 replyer 配置的 `deepseek-v4-flash-0731`。
* 正文与「引用」内容都会翻译并被替换，推文里不再出现原文；
  话题标签、@账号、链接按提示词要求保持原样。
* 本来就是目标语言的推文跳过翻译（优先看接口给的 `lang`，拿不到时按字符比例兜底）。
* 超长推文不翻译（默认 1200 字符）以省 token；单条翻译默认 45 秒超时。
* 译文按「推文 ID + 目标语言」缓存，多个群推送与失败重试都不会重复调用模型。
* 翻译任何环节失败（超时 / 模型报错 / 空结果）都静默保留原文，绝不因为翻译丢推文。

## 1.2.0

一次推送多条推文时，改成「一条聊天记录装多条推文」：

* 新增 `display.batch_forward`（默认开）：一轮轮询到的多条新推文打包成**一条合并转发**，
  **每条推文占其中一个节点**（节点昵称用该推文的作者名）。以前是每条推文各发一条合并转发，
  三条新推文就会刷屏三条消息。
* 单条推文保持轻量格式：纯文字发普通消息，不必让人点开聊天记录。
* 新增 `display.batch_max_mb`（默认 7MB）：单条聊天记录里的图片总量超限时自动拆成多条，
  节点数也限制在 20 个以内，避免撞上插件 IPC 单帧 16MB 限制。
* 新增 `display.video_in_forward`（默认开）：视频也放进这条聊天记录里。
  注意 QQ 协议规定「video 必须是消息里唯一的元素」（实测报错
  `message element "video" must be the only segment in a message`），
  所以视频推文会占**两个相邻节点**：先是正文节点，紧跟一个只有视频的节点。
  关掉这个开关则视频改成单独成条发。
* 合并转发整体失败时逐条回退成普通图文，格式问题绝不导致丢推文。
* 多聊天流订阅时，图片只下载一次，多个群复用同一份。

### 关于「嵌套聊天记录」

需求里设想过「外层聊天记录里，每条推文再套一条聊天记录」的更深一层嵌套。
实测这条路走不通：适配器不会转换节点内的 `forward` 段，会打出
`SnowLuma 跳过无法转换的出站消息段: type=forward`，整条消息因为没有任何可转换段而发送失败。
因此最终采用「扁平多节点 + 视频单独占一个节点」的结构，这也是本客户端能表达的最接近形态。

## 1.1.0

原推带视频时不再只发封面，改成尽量把原视频搬过来：

* 新增 `media.video_mode = "auto"`（默认）：推文有视频时先尝试直接发原视频，
  发不出去才回退成「封面 + 链接」；`thumbnail` / `link` 保持原来的行为。
* 视频变体选择：解析 fxtwitter 返回的全部 mp4 变体（480p/720p/1080p/4K），
  逐个 HEAD 取真实大小，挑上限内码率最高的那个，避免默认拖 4K 巨物。
* 两条投递路线：
  * **内联**（≤ `inline_video_mb`，默认 10MB）：下载后以 base64 直接发，
    受 MaiBot 插件 IPC 单帧 16MB 限制，代码里硬性夹到 11MB。
  * **容器借道**（≤ `max_video_mb`，默认 300MB）：下载到本地后 `docker cp`
    进 SnowLuma 容器，以容器内路径发送，发完自动清理容器与本地临时文件。
* 大小、时长、码率都拿不到时退化为「按码率×时长估算」，实在不行交给下载环节按上限丢弃。
* 新增 `_cleanup_video_dir`：插件启动时清掉崩溃残留的临时视频。
* 修掉一个自己写出来的 bug：大视频路由漏了容器内文件清理（测试用例逮到的）。
* 顺带修文案：走视频路径时不再声称「配图 N 张」。

## 1.0.1

修复用户名写错时的提示：

* 以前只要有一个用户名解析不出来就统一回一句「用法：/tw_sub <用户名>」，看起来像没输入参数。
  现在会逐条说明原因：超长（并给出字符数上限）、含非法字符、不是 X 链接、纯数字 ID 等。
* 超长或明显手误时，用"删掉某个下划线"等候选去问接口，猜对了就提示
  「你是不是想订阅 @LimbusCompany_B？」（例：`limbus_company_b` → `LimbusCompany_B`）。
* 一次写多个用户名时，有效的照常处理，无效的单独在结果里标注「已跳过」。
* `/tw_unsub`、`/tw_reset`、`/tw_del`、`/tw_check` 在推主没订阅时，会提示最接近的已订阅推主（手误纠正）。

## 1.0.0

首个版本：

* 用 FxTwitter / FxEmbed 轮询订阅推主的最新推文，默认间隔 10 分钟（可配置、可用命令覆盖）。
* 新推文按「正文 + 配图 + 原推链接 + 互动数据」转发到订阅它的聊天流，默认打包成一条合并转发。
* 主接口为 v2 JSON API，失败时自动回退 Atom feed；图片经代理下载后以 base64 发送。
* 按推文 ID 去重并持久化状态，重启不重复推送；首次订阅只建基线、不补推历史。
* 13 个斜杠命令覆盖订阅管理（sub / unsub / list / all / on / off）、运行控制
  （check / test / interval / reset / del）与状态查询（status / help）。
* 注册只读工具 `twitter_latest` 供 LLM 查询推主最新推文，并在 WebUI 首页提供入口卡片。
